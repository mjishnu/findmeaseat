# Dev/test dependencies. The production image installs only requirements.txt,
# so pytest never ships in the container. Install locally with:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.4.2  # pinned <9 for pytest-asyncio 1.2.0 compatibility
pytest-asyncio==1.2.0  # async test support (asyncio_mode=auto)
